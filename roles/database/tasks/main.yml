#  - name: "Create postgres config"
#    template: src="templates/postgres_config.sql.j2"

- name: "Launch postgres container"
  docker:
    name: db
    image: postgres
    env:
      POSTGRES_USER: "{{credentials.postgres.db_user}}"
      POSTGRES_PASSWORD: "{{credentials.postgres.db_pword}}"
    ports:
      - "{{ports.postgres.sql}}:5432"

